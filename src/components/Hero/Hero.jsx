import React from 'react'
import { HeroContainer, HeroText, HeroWrapper } from './HeroStyles'

const Hero = () => {
    return (
        <HeroContainer>
            <HeroWrapper>
                <HeroText>
                    <a href="">sé tendencia</a>
                    <h1>tu marca en boca de todos</h1>
                </HeroText>
            </HeroWrapper>
        </HeroContainer>
    )
}

export default Hero